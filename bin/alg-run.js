const path = require('path')
const fs = require('fs')
const map = require('./map')
const { root, checkIfProblemExists } = require('./util')

function runAlg({ name, number, input }) {
  console.log(argv)
}

module.exports = runAlg
